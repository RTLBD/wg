# Copy to .env and set a strong password before: docker compose up -d
POSTGRES_USER=wgeasy
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=wgeasy

# Optional: pin image tag (default: latest)
# WG_IMAGE_TAG=15.3.0
